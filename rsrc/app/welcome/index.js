(() => {

  let $=new Fx({

		//===========================================================[init]=> override formatrix-init config
		init:{

		},
		//===========================================================[core]=> override formatrix-core setup
		core:null,
	});
	//************************************************************************>
	$.use({

	});
	//************************************************************************>
	$.run(function(){

		console.log("%c[fx:app:welcome] ", "color:#5a5;");

	});
	//************************************************************************>
})();
